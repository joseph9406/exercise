<% if @event.errors.any? %>
  <ul>
    <% @event.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
  </ul>
<% end %>

<%= f.label :name, "名稱" %>  
<%= f.text_field :name %>

<%= f.label :description, "描述" %>
<%= f.text_area :description %>

  
